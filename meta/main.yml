galaxy_info:
  author: misva
  description: Ansible role that sets up tflint utility
  company:

  license: BSD

  min_ansible_version: 2.2

  platforms:
  - name: EL
    versions:
    - 7

  galaxy_tags: [ "CentOS", "RedHat", "tflint", "terraform" ]

dependencies:
  - mauromedda.ansible_role_unzip
